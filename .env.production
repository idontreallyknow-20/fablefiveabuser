# Public build-time configuration. Safe to commit: the Supabase URL and
# publishable key are public by design (RLS protects all data).
# Secrets (SUPABASE_SERVICE_ROLE_KEY, SPOTIFY_*, GOOGLE_*) must NEVER go in
# this file; set them in the Vercel dashboard.
NEXT_PUBLIC_SUPABASE_URL=https://tezmxgexbqqgvpbkixzp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_at3E9oX8IV2dNK99-wpRqw_vQctW3iB
PUBLIC_SIGNUPS_ENABLED=false
# Production URL, used to build OAuth redirect URIs. Must match the domain
# registered in the Spotify and Google consoles exactly (no trailing slash).
NEXT_PUBLIC_APP_URL=https://aesthetic-inky-seven.vercel.app
